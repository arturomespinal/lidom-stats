# HTTP & Scraping
requests==2.32.3
httpx==0.27.0
beautifulsoup4==4.12.3
lxml==5.2.2
selenium==4.21.0
playwright==1.44.0

# Datos & ETL
pandas==2.2.2
pydantic==2.7.1

# Base de datos
SQLAlchemy==2.0.30

# Utilidades
python-dotenv==1.0.1
tenacity==8.3.0      # Reintentos automáticos en scraping
loguru==0.7.2        # Logging profesional

# Desarrollo
pytest==8.2.0
jupyter==1.0.0
ipykernel==6.29.4

# Scheduler
apscheduler==3.10.4

# API
fastapi==0.111.0
uvicorn==0.30.1

# Motor en vivo
# El feed de MLB entrega los cambios como JSON Patch (RFC 6902). Implementar
# el estándar a mano —add/remove/replace/move/copy, índices de array, el "-"
# de append— es un problema resuelto, y equivocarse en un detalle corrompe el
# estado en silencio, que es el peor modo de fallo posible aquí.
jsonpatch==1.33

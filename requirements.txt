# Cliente HTTP
# httpx y no requests: soporta HTTP/2, timeouts por fase y un cliente con
# sesión reutilizable, con una API casi idéntica. Ver src/clients/mlb_api.py.
httpx==0.27.0
tenacity==8.3.0      # Reintentos con backoff exponencial contra la MLB API

# Datos & ETL
pydantic==2.7.1

# Base de datos
SQLAlchemy==2.0.30

# Utilidades
loguru==0.7.2

# API
fastapi==0.111.0
uvicorn==0.30.1

# Motor en vivo
# El feed de MLB entrega los cambios como JSON Patch (RFC 6902). Implementar
# el estándar a mano —add/remove/replace/move/copy, índices de array, el "-"
# de append— es un problema resuelto, y equivocarse en un detalle corrompe el
# estado en silencio, que es el peor modo de fallo posible aquí.
jsonpatch==1.33
